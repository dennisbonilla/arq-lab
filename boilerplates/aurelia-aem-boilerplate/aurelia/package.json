{
  "name": "aurelia-boilerplate",
  "version": "1.0.0",
  "private": true,
  "description": "Front-end boilerplate (React + Node) designed to be unpacked into AEM components.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently -n api,web -c green,yellow \"npm:dev:server\" \"npm:dev:client\"",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
